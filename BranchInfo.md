# Test Branch Snapshot

This branch (`Test`) mirrors the current VR Quest 2 runtime loading prototype so you can review or extend it separately from the main work branch. All project files are included.

## Notes
- Based on Unity 2022.3 LTS settings with XR Interaction Toolkit 2.5.4 and XR Core Utils 2.3.0 already referenced in `Packages/manifest.json`.
- Sample scene `Assets/Scenes/SampleLoaderScene.unity` contains the loader wiring and default material.
- Use this branch to experiment without impacting the main branch; continue normal development from here as needed.
